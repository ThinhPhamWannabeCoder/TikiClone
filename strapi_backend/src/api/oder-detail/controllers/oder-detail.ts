/**
 * oder-detail controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::oder-detail.oder-detail',({strapi})=>({
    // GET PAGINATION ORDER DETAIL

    // UPDATE ORDER STATUS

    
}));
