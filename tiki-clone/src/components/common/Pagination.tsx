interface myProp{
    url: string,
    page: number,
    total: number,
    
}
export default function Pagination(prop:myProp){
    // Render Url
    // Swtich Active ()
    // If too long -> Logic rendering
    return(
        <div className="w-full bg-gray-400 rounded-lg p-4">
            this is pagiantion
        </div>
    )
}